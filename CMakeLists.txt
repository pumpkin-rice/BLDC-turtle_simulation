cmake_minimum_required(VERSION 3.12)

project(BLDC_turtle_simulation)

set(RUNTIME_OUTPUT_DIRECTORY ${CMAKE_SOURCE_DIR})

# Include toolchain file

# add_subdirectory(s-func)
add_subdirectory(mex)
